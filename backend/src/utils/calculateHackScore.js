/**
 * Calculate HackScore for a student across all hackathons.
 * @param {string} userId
 * @returns {Promise<number>} Calculated HackScore
 */
const calculateHackScore = async (userId) => {
    // TODO: implement scoring algorithm
};

module.exports = { calculateHackScore };
